# 📱 راهنمای نصب و اجرای RoboBet در Termux

این راهنما نحوه نصب و اجرای پروژه RoboBet را در محیط Termux موبایل توضیح می‌دهد.

## ⚠️ پیش‌نیازها

1. **نصب Termux**: اپلیکیشن Termux را از F-Droid یا GitHub نصب کنید (نسخه Google Play قدیمی است و توصیه نمی‌شود)
   - لینک دانلود: https://f-droid.org/en/packages/com.termux/

2. **فضای ذخیره‌سازی**: حداقل 500MB فضای خالی

3. **اتصال اینترنت**: برای دانلود پکیج‌ها و اجرای ربات

## 🚀 مراحل نصب

### 1. کپی پروژه به گوشی

**روش اول: استفاده از Git (توصیه می‌شود)**

```bash
pkg install git
git clone https://github.com/javad-sh/RoboBet.git
cd RoboBet
```

**روش دوم: کپی دستی فایل‌ها**

فقط این فایل‌ها را به گوشی منتقل کنید:

📁 **فایل‌های ضروری:**
- ✅ `main.py` - اسکریپت اصلی
- ✅ `bot.py` - ربات تلگرام
- ✅ `requirements.txt` - لیست پکیج‌ها
- ✅ `setup_termux.sh` - اسکریپت نصب
- ✅ `start_termux.sh` - اسکریپت اجرا

📁 **فایل‌های اختیاری** (خودکار ساخته می‌شوند):
- `chat_ids.json`
- `betforward_odds.json`
- `betforward_results.json`

❌ **فایل‌های غیرضروری** (منتقل نکنید):
- `Dockerfile`, `supervisord.conf`, `.git/`, گزارش‌ها، یادداشت‌ها

**مراحل انتقال:**
1. فایل‌های بالا را در یک پوشه (مثلاً `RoboBet`) در گوشی قرار دهید
2. پوشه را در `Download` یا هر جای دیگری در حافظه داخلی کپی کنید
3. سپس در Termux دستورات زیر را اجرا کنید:

```bash
# ابتدا دسترسی به حافظه را فعال کنید
termux-setup-storage

# کپی پروژه از حافظه داخلی به Termux
cp -r /sdcard/Download/RoboBet ~/
cd ~/RoboBet

# بررسی فایل‌ها
ls -la
```

⚠️ **هشدار**: مستقیماً از `/sdcard/` اجرا نکنید! فایل‌ها را حتماً به `~/` کپی کنید زیرا پارتیشن SD Card از دسترسی‌های اجرایی (execute permissions) پشتیبانی نمی‌کند.

### 2. دسترسی به حافظه داخلی (مهم!)

⚠️ **قبل از هر کاری، این دستور را اجرا کنید:**

```bash
termux-setup-storage
```

این دستور دسترسی Termux به حافظه داخلی گوشی را فعال می‌کند. پس از اجرا، دکمه "Allow" را بزنید.

### 3. کپی فایل‌ها به دایرکتوری خانگی Termux

اگر فایل‌ها در `/sdcard` هستند، آنها را به دایرکتوری Termux کپی کنید:

```bash
# کپی پروژه از حافظه داخلی به Termux
cp -r /sdcard/Download/RoboBet ~/
cd ~/RoboBet
```

⚠️ **نکته**: فایل‌های روی `/sdcard` نمی‌توانند دسترسی اجرا (`execute permission`) داشته باشند. حتماً آنها را به دایرکتوری خانگی Termux کپی کنید.

### 4. اجرای اسکریپت نصب

```bash
chmod +x setup_termux.sh
bash setup_termux.sh
```

اگر همچنان خطای Permission Denied دریافت کردید:
```bash
# اجرا با bash به صورت مستقیم
bash setup_termux.sh
```

این اسکریپت موارد زیر را انجام می‌دهد:
- به‌روزرسانی پکیج‌های Termux
- نصب Python، Chromium و ChromeDriver
- نصب کتابخانه‌های Python مورد نیاز
- ایجاد فایل‌های JSON اولیه

⏱️ **زمان نصب**: حدود 10-15 دقیقه (بسته به سرعت اینترنت)

### 5. تنظیم توکن ربات (اختیاری)

توکن ربات تلگرام شما در فایل‌های `main.py` و `bot.py` قبلاً تنظیم شده است. اگر می‌خواهید آن را تغییر دهید:

```bash
nano main.py  # یا از ویرایشگر دلخواه استفاده کنید
# خط BOT_TOKEN را پیدا کرده و مقدار آن را تغییر دهید
```

همین کار را برای `bot.py` نیز انجام دهید.

## ▶️ اجرای ربات

برای شروع ربات:

```bash
chmod +x start_termux.sh
./start_termux.sh
```

خروجی به شکل زیر خواهد بود:
```
Starting RoboBet...
Starting Telegram bot (bot.py)...
Bot PID: 12345
Starting main scraper (main.py)...
Main PID: 12346
==========================================
RoboBet is running!
==========================================
Bot PID: 12345
Main PID: 12346

Press CTRL+C to stop all processes
==========================================
```

## 🛑 متوقف کردن ربات

برای متوقف کردن ربات، کلیدهای `Ctrl+C` را در ترمینال فشار دهید.

## 📝 نکات مهم

### 🔋 مصرف باتری
- استفاده از Chromium و Selenium مصرف باتری بالایی دارد
- توصیه می‌شود گوشی را به شارژر وصل کنید
- برای کاهش مصرف باتری، می‌توانید فاصله زمانی اسکرپ کردن را در `main.py` افزایش دهید

### 📶 اتصال اینترنت
- ربات نیاز به اتصال مداوم اینترنت دارد
- از Wi-Fi برای کاهش مصرف دیتا استفاده کنید

### 💾 فضای ذخیره‌سازی
- فایل‌های JSON نتایج در همان دایرکتوری ذخیره می‌شوند
- به صورت دوره‌ای فایل‌های لاگ را پاک کنید

### 🔄 اجرای در پس‌زمینه
برای اجرای ربات در پس‌زمینه و جلوگیری از بسته شدن آن:

```bash
# استفاده از tmux (توصیه می‌شود)
pkg install tmux
tmux new -s robobet
./start_termux.sh

# برای جدا شدن از session: Ctrl+B سپس D
# برای بازگشت به session:
tmux attach -t robobet
```

یا استفاده از nohup:
```bash
nohup ./start_termux.sh > output.log 2>&1 &
```

### 🐛 عیب‌یابی

**مشکل: Chromium اجرا نمی‌شود**
```bash
# نصب مجدد Chromium
pkg reinstall chromium
```

**مشکل: خطای Permission Denied**

این خطا معمولاً به دلایل زیر رخ می‌دهد:

1. **فایل‌ها روی `/sdcard` هستند:**
```bash
# راه‌حل: کپی فایل‌ها به دایرکتوری خانگی
termux-setup-storage
cp -r /sdcard/Download/RoboBet ~/
cd ~/RoboBet
chmod +x *.sh
```

2. **فایل دسترسی اجرا ندارد:**
```bash
# اضافه کردن دسترسی اجرا
chmod +x setup_termux.sh start_termux.sh

# یا اجرا مستقیم با bash
bash setup_termux.sh
```

3. **خط‌های پایان (Line Endings) اشتباه است:**
```bash
# نصب dos2unix برای تبدیل فرمت
pkg install dos2unix
dos2unix *.sh
chmod +x *.sh
```

4. **بررسی مسیر فعلی:**
```bash
pwd  # باید ~/RoboBet یا مسیر مشابه باشد، نه /sdcard/...
ls -la *.sh  # بررسی دسترسی‌های فایل
```

**مشکل: پکیج Python نصب نمی‌شود**
```bash
pip install --upgrade pip
pip install -r requirements.txt --no-cache-dir
```

**مشکل: خطای "Chrome binary not found" یا "Unable to obtain driver"**

این خطا نشان می‌دهد که Chromium یا ChromeDriver نصب نشده است:

```bash
# نصب هر دو پکیج ضروری
pkg install chromium chromedriver

# بررسی نصب موفق
which chromium-browser
which chromedriver

# اگر هنوز مشکل دارید، مسیرها را چک کنید
ls -la /data/data/com.termux/files/usr/bin/chromium*
ls -la /data/data/com.termux/files/usr/bin/chromedriver

# اگر chromedriver نصب نشد، از مخزن دیگری امتحان کنید
pkg update
pkg upgrade
pkg install chromium chromedriver -y
```

**اگر همچنان خطا دارید:**
```bash
# پاک کردن کش
pkg clean
rm -rf $PREFIX/var/cache/apt/archives/*

# نصب مجدد
pkg reinstall chromium chromedriver
```

## 📊 بررسی وضعیت

برای مشاهده لاگ‌ها:
```bash
# مشاهده PID پروسس‌ها
ps aux | grep python

# مشاهده فایل‌های JSON
cat betforward_odds.json
cat betforward_results.json
cat chat_ids.json
```

## 🔧 تنظیمات پیشرفته

### تغییر فاصله زمانی اسکرپ

در فایل `main.py`:
```python
# پیدا کردن این خطوط:
schedule.every(20).minutes.do(scrape_odds_job)
schedule.every(5).minutes.do(scrape_results_job)

# تغییر به دلخواه (به دقیقه):
schedule.every(30).minutes.do(scrape_odds_job)  # هر 30 دقیقه
schedule.every(10).minutes.do(scrape_results_job)  # هر 10 دقیقه
```

### غیرفعال کردن Headless Mode (برای عیب‌یابی)

در فایل `main.py`، خط `chrome_options.add_argument("--headless")` را کامنت کنید:
```python
# chrome_options.add_argument("--headless")
```

## 🆚 تفاوت‌ها با نسخه Railway

| ویژگی | Railway | Termux |
|-------|---------|--------|
| Chrome/Chromedriver | مسیر ثابت `/usr/bin/` | Auto-detect یا `/data/data/com.termux/` |
| Process Manager | Supervisor | Script با PID tracking |
| Deployment | Docker Container | مستقیم روی سیستم |
| Auto-restart | بله (Supervisor) | خیر (نیاز به tmux/nohup) |
| Resources | Server-grade | محدود به موبایل |

## ❓ سوالات متداول

**Q: آیا می‌توانم گوشی را قفل کنم؟**
A: بله، اما برخی گوشی‌ها ممکن است Termux را متوقف کنند. از قفل کردن Termux در تنظیمات Battery Optimization جلوگیری کنید.

**Q: آیا Root نیاز است؟**
A: خیر، Root نیاز نیست.

**Q: چقدر RAM مصرف می‌کند؟**
A: حدود 300-500MB بسته به تعداد مسابقات در حال پردازش.

**Q: آیا می‌توانم چند نمونه اجرا کنم؟**
A: بله، اما توصیه نمی‌شود زیرا منابع محدود است.

## 📞 پشتیبانی

در صورت بروز مشکل:
1. لاگ‌های خطا را بررسی کنید
2. اطمینان حاصل کنید که تمام پکیج‌ها نصب شده‌اند
3. Termux و پکیج‌ها را به‌روز کنید: `pkg upgrade`

---

**نکته امنیتی**: توکن ربات تلگرام خود را با کسی به اشتراک نگذارید و از قرار دادن آن در مخازن عمومی خودداری کنید.
